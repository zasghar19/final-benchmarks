(define (a x) x)
(define (b x) (a x))
(define (c x) (b x))
(define (d x) (c x))
(define (e x) (d x))
(define (f x) (e x))
(define (g x) (f x))
(define (h x) (g x))
(define (i x) (h x))
(define (j x) (i x))
(define (k x) (j x))
(define (l x) (k x))
(define (m x) (l x))
(define (n x) (m x))
(define (o x) (n x))
(define (p x) (o x))
(define (q x) (p x))
(define (r x) (q x))
(define (s x) (r x))
(define (t x) (s x))
(define (u x) (t x))
(define (v x) (u x))
(define (w x) (v x))
(define (x x) (w x))
(define (y x) (x x))
(define (z x) (y x))
(print (z 10))
